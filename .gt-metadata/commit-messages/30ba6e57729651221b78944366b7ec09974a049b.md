#### Code
```kotlin
package com.glassthought.sandbox

import com.glassthought.sandbox.util.benchmarker.Benchmarker
import kotlinx.coroutines.runBlocking
import kotlin.concurrent.thread

fun main(args: Array<String>) {
  thread {
    runBlocking {
      val result = Benchmarker.benchmark(
        {
          Thread.currentThread().name
        },
        name = "Thread.currentThread().name",
      )

      println(result)
    }
  }
}
```

#### Command to reproduce:
```bash
gt.sandbox.checkout.commit 30ba6e57729651221b78 \
&& cd "${GT_SANDBOX_REPO}" \
&& cmd.run.announce "./gradlew run --quiet"
```

#### Recorded output of command:
```txt
=== Benchmark Results for Thread.currentThread().name ===
Executed 1000000 times
Total time:  17.621 milliseconds
Average:     17 ns
Min:         0 ns
Max:         22.583 microseconds
P50:         0 ns  ((âˆ runs/ms))
P90:         42 ns  ((23809.524 runs/ms))
P99:         84 ns  ((11904.762 runs/ms))
P99.9:       291 ns  ((3436.426 runs/ms))
P100:        22.583 microseconds  ((44.281 runs/ms))
```
